POTTERY -> NORWEGIAN PDF — scaffold (v8)
v8 adds:
 - styles/page.css : ONE shared stylesheet every page links (identical look set-wide)
 - figures auto-match the crop's aspect ratio (no distortion/padding)
 - reimagined figures are REUSED across runs (no re-charge); force with: /rebuild <name> remake-art
 - QA screenshot tightened to true A4
Restart Claude Code after updating so changed agents load.
